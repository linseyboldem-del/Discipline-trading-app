# Your deployed backend URL (Render), or http://localhost:8787 for local testing
BACKEND_URL=http://localhost:8787

# Must match SCANNER_INGEST_KEY in the backend's .env exactly
SCANNER_INGEST_KEY=change-me-to-a-long-random-string
